<!-- Bleibt fürs erste, wird vllt entfernt -->

<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/html">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" charset="UTF-8">
        <link rel="stylesheet" href="../static/registerLogin">
        <script src="http://localhost:35729/livereload.js" rel="preload" async></script>
        <title>Registrierung / Anmeldung</title>
    </head>
    <body>
        <div class="container">
            <h1 style="color: White" id="form-title">Anmeldung</h1>
            <form action="mainMenu" method="post" id="auth-form">

                <label style="color: White" for="username">Benutzername:</label>
                <input type="text" name="username" required>
                <label style="color: White" for="password">Passwort:</label>
                <input type="password" name="password" required>
                <button type="submit" id="form-button">Anmelden</button>
            </form>
                <button onclick="switchForm()" id="switch-button">Registrierung</button>
        </div>
        <script>
            let isRegister = false;
            function switchForm()
            {
                isRegister = !isRegister;
                document.getElementById('form-title').innerText = isRegister ? 'Registrierung' : 'Anmeldung';
                document.getElementById('form-button').innerText = isRegister ? 'Registrieren' : 'Anmelden';
                document.getElementById('switch-button').innerText = isRegister ? 'Zurück zur Anmeldung' : 'Registrierung';
            }
        </script>
    </body>
</html>